import React from "react";
import "../css/navbar.css"

export const Navbar = ({theme}) => {
  return <nav>
    <h1>devfinder</h1>
    <div id="themeToggle">
        
    </div>
  </nav>;
};

export default Navbar;
